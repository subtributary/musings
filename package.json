{
  "name": "musings-web-assets",
  "version": "0.1.0",
  "private": true,
  "description": "Frontend asset build pipeline for Musings",
  "scripts": {
    "dev": "vite",
    "build:server": "tsc && vite build",
    "build": "npm run build:cms && npm run build:server"
  },
  "devDependencies": {
    "@subtributary/drip": "^0.2.0",
    "sass": "^1.77.0",
    "typescript": "~6.0.2",
    "vite": "^8.1.4"
  }
}

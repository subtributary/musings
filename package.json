{
  "name": "musings-web-assets",
  "version": "0.1.0",
  "private": true,
  "description": "Build pipeline for SCSS (and optional JS/TS later)",
  "scripts": {
    "build:css": "sass --load-path=node_modules web/src/scss:web/static/css --no-source-map",
    "build:watch": "sass --watch --load-path=node_modules web/src/scss:web/static/css --no-source-map",
    "build": "npm run build:css"
  },
  "devDependencies": {
    "@subtributary/drip": "^0.2.0",
    "sass": "^1.77.0"
  }
}

package posts

/*
var config = []search.FieldConfig{
	{
		Name:   "Title",
		Weight: 4.0,
		B:      0.75,
	},
	{
		Name:   "Content",
		Weight: 1.0,
		B:      0.75,
	},
}

// Index is a facade over our internal search algorithm.
type Index struct {
	index search.Index
}

func NewIndex() Index {
	return Index{
		index: search.NewIndex(config),
	}
}

func (idx Index) Search(query string) {
	// not yet implemented
}

func (idx Index) Upsert(filename string, post ParsedPost) {
	idx.index.Upsert(filename, map[search.Field]string{
		"Title":   post.Title,
		"Content": post.Content,
	})
}
*/
